<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Select Network:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:paddingBottom="16dp" />

        <!-- Network Selection (BSC Only) -->
        <RadioGroup
            android:id="@+id/networkSelector"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="16dp">

            <RadioButton
                android:id="@+id/bscRadio"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="BSC (BEP20)"
                android:textSize="16sp"
                android:checked="true"
                android:enabled="true" />

        </RadioGroup>

        <!-- CRITICAL WARNING -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="#FFF3E0"
            android:padding="16dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="⚠️ IMPORTANT - READ CAREFULLY"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#E65100"
                android:paddingBottom="8dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="• Send ONLY USDT (BEP20 token)\n• Use BSC Network ONLY\n• Wrong network = Lost funds forever!\n• Double-check the address\n• Transaction is irreversible"
                android:textSize="14sp"
                android:textColor="#BF360C"
                android:lineSpacingExtra="4dp" />

        </LinearLayout>

        <!-- Plan Selection -->
        <Button
            android:id="@+id/monthlyButton"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="Monthly - $2 USDT"
            android:textSize="18sp"
            android:textColor="@android:color/white"
            android:backgroundTint="#6200EA"
            android:layout_marginBottom="16dp" />

        <Button
            android:id="@+id/yearlyButton"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="Yearly - $20 USDT"
            android:textSize="18sp"
            android:textColor="@android:color/white"
            android:backgroundTint="#6200EA"
            android:layout_marginBottom="24dp" />

        <!-- Enter License Key -->
        <Button
            android:id="@+id/enterLicenseButton"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="Enter License Key"
            android:textSize="18sp"
            android:textColor="#6200EA"
            android:backgroundTint="@android:color/white"
            android:background="?attr/selectableItemBackground"
            android:layout_marginBottom="16dp" />

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

        <!-- Error Text -->
        <TextView
            android:id="@+id/errorText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#B00020"
            android:textSize="14sp"
            android:gravity="center"
            android:visibility="gone"
            android:padding="8dp" />

        <!-- Logout / Switch Account Button -->
        <Button
            android:id="@+id/logoutButton"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="Switch Account"
            android:textSize="14sp"
            android:textColor="#757575"
            android:backgroundTint="@android:color/transparent"
            android:background="?attr/selectableItemBackground"
            android:layout_marginTop="24dp" />

    </LinearLayout>

</ScrollView>
